<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:padding="16dp">

        <!-- TextView for App name -->
        <TextView
            android:id="@+id/appName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textSize="16sp"
            android:paddingBottom="32dp"
            android:textStyle="bold"
            android:gravity="center"
            />

        <!-- TextView for App Overview -->
        <TextView
            android:id="@+id/appOverview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/app_overview"
            android:textSize="16sp"
            android:paddingBottom="32dp"
            />

        <!-- TextView for Selection prompt -->
        <TextView
            android:id="@+id/selectionPrompt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/selection_prompt"
            android:textSize="16sp"
            android:paddingBottom="6dp"
            />

        <!-- Spinner for selecting FAANG companies -->
        <Spinner
            android:id="@+id/companySpinner"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:minHeight="48dp"
            />

        <!-- Button for Overview Activity -->
        <Button
            android:id="@+id/overviewButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Overview" />

        <!-- Button for Details Activity -->
        <Button
            android:id="@+id/detailsButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Details" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>